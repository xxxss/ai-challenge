function update(car, world) {

	// Object ai contains auxiliary functions

	ai.log(DATA); // Outputs in a special window of the required data
	// Supports output variables, arrays, objects

	return car;
}
        	
